Compile timetest:
g++ -O3 -std=c++14 ../cell.h ../cellsystem.h ../cellsystem.cpp timetest.cpp -o timetest

Compile logictest:
g++ -O3 -std=c++14 ../cell.h ../cellsystem.h ../cellsystem.cpp logictest.cpp -o logictest

Compile timetest (multithreading):
g++ -O3 -std=c++14 ../cell.h ../cellsystem.h ../cellsystem.cpp timetest.cpp -o timetest_threads -pthread

Compile logictest (multithreading):
g++ -O3 -std=c++14 ../cell.h ../cellsystem.h ../cellsystem.cpp logictest.cpp -o logictest_threads -pthread

To enable multithreading-support please remove "//" in front of "#define THREADS" in cellsystem.h.
After that compile again with the multithreading compile command.

To change the used algorithm, replace in the line "#define ALG1" in cellsystem.h the 1 with 2 or 3.
After that you have to compile the tests again.
